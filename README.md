# Oystercard

## Testing

Within the command line, inside the directory, type rspec to run the tests.

1. We wrote a test to check that the initial balance of a card would be 0. We set it up inside the initialized method with an attr_reader to access it and get rid of the potential balance method.
